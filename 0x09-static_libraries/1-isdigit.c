#include "main.h"

/**
 * _isdigit -this will  checks for a digit (0 through 9)
 * @c:is the int that will  be checked
 * Return: 1 when c is a digit and 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

#include "main.h"
/**
 * _strlen -t will returns the length of a string
 * @s:is a string
 * Return: length
 */
int _strlen(char *s)
{
	int lenght = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}

	return (length);
}

